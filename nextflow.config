singularity {
    enabled = true
    autoMounts = true
}

trace {
  Fields = '''
           task_id,
           hash,
           name,
           status,
           duration,
           %cpu,
           peak_vmem,workdir
  '''
}

